---
all:
  hosts:
    horrea:
    oculus:
    coliseum:
      ansible_connection: local
    localhost:
      ansible_connection: local
    # wsl is configured to use "mirrored" networking,
    # so reference the windows host name; not the linux hostname
    surface:
